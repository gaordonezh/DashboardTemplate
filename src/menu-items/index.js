import { other } from "./other";
import { principal } from "./principal";

//-----------------------|| MENU ITEMS ||-----------------------//

const menuItems = {
  items: [principal, other],
};

export default menuItems;
